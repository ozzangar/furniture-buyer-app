# Copy to .env and fill in real values. .env is git-ignored — never commit real keys.

# Shop API (issued by organizers on the day)
SHOP_API_BASE_URL=https://example-shop-api.invalid
SHOP_API_KEY=your-api-key-here
SHOP_USER_ID=u000

# Flask
FLASK_SECRET_KEY=change-me-to-a-random-string
